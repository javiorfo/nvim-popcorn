local content = {
        { "Project Metadata: ", "Type" }, { "lala" },
        { "   Group    =>" },
        { "   Artifact =>" },
        { "   Name     =>" },
        { "Language    =>" },
    }

for _, b in pairs(content) do
--     for _, d in pairs(b) do
       print(b[1])
--     end
end
